import { SetMetadata } from '@nestjs/common';
// import { Permission } from 'src/authentication/authorization/permission';

// export const PERMISSION_KEY = 'permissions';
// export const RequirePermissons = (...permissions: Permission[]) => SetMetadata(PERMISSION_KEY, permissions);
